# Railway Environment Variables Template
# ⚠️ SECURITY: Replace all placeholders with your actual values from Supabase
# Add these in Railway → Your Service → Variables tab

PORT=3001
NODE_ENV=production

# Supabase Configuration
# Get these from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY_HERE

# Database Connection (Direct)
# Get these from: Supabase Dashboard → Settings → Database → Connection string
DB_HOST=db.YOUR_PROJECT_REF.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=YOUR_DATABASE_PASSWORD_HERE
DB_SSL=true

# Supabase Database (Alternative naming)
SUPABASE_DB_NAME=postgres
SUPABASE_USER=postgres
SUPABASE_PASSWORD=YOUR_DATABASE_PASSWORD_HERE
SUPABASE_SSL=true
SUPABASE_PROJECT_REF=YOUR_PROJECT_REF_HERE

# Connection Pooler (Recommended - avoids IPv6 issues)
# The config will use the pooler by default if DB_USE_POOLER is not set to 'false'
DB_USE_POOLER=true
DB_POOLER_HOST=aws-1-ap-south-1.pooler.supabase.com

# OAuth (Optional - Add later when implementing)
# LINKEDIN_CLIENT_ID=your_linkedin_client_id
# LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret

# Gemini AI (Optional - Add later when implementing)
# GEMINI_API_KEY=your_gemini_api_key

